# Display Google - セットアップガイド

このガイドでは、Display Googleアプリケーションを実行するまでの手順を詳しく説明します。

## ステップ1: 依存関係のインストール

プロジェクトディレクトリで以下のコマンドを実行してください:

```bash
npm install
```

これにより、必要なすべてのパッケージがインストールされます。

## ステップ2: Google Cloud Consoleの設定

### 2.1 プロジェクトの作成

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. 右上のプロジェクト選択ドロップダウンをクリック
3. 「新しいプロジェクト」をクリック
4. プロジェクト名を入力(例: "DisplayGoogle")
5. 「作成」をクリック

### 2.2 APIの有効化

1. 左メニューから「APIとサービス」 > 「ライブラリ」を選択
2. 検索バーで「Google Calendar API」を検索
3. 「Google Calendar API」をクリックし、「有効にする」をクリック
4. 同様に「Google Tasks API」も検索して有効化

### 2.3 OAuth 2.0認証情報の作成

1. 左メニューから「APIとサービス」 > 「認証情報」を選択
2. 上部の「+ 認証情報を作成」をクリック
3. 「OAuth クライアント ID」を選択
4. 初回の場合、「OAuth同意画面を構成」をクリック
   - ユーザータイプ: 「外部」を選択
   - アプリ名: "Display Google"
   - ユーザーサポートメール: あなたのメールアドレス
   - デベロッパーの連絡先情報: あなたのメールアドレス
   - 「保存して次へ」をクリック
   - スコープは設定不要、「保存して次へ」をクリック
   - テストユーザーにあなたのGoogleアカウントを追加
   - 「保存して次へ」をクリック
5. 再度「+ 認証情報を作成」 > 「OAuth クライアント ID」を選択
6. アプリケーションの種類: 「デスクトップ アプリ」を選択
7. 名前: "Display Google Desktop"
8. 「作成」をクリック
9. 表示されたクライアントIDとクライアントシークレットをコピー

### 2.4 リダイレクトURIの設定

1. 作成した認証情報をクリック
2. 「承認済みのリダイレクト URI」セクションで「+ URIを追加」をクリック
3. `http://localhost:3000/oauth2callback` を入力
4. 「保存」をクリック

## ステップ3: 認証情報の設定

### 3.1 開発環境での設定方法（.envファイル）

**推奨方法** - 機密情報を安全に管理できます。

1. プロジェクトディレクトリで `.env.example` をコピーして `.env` を作成:
   ```bash
   cp .env.example .env
   ```

2. `.env` ファイルを編集し、取得した値を入力:
   ```
   GOOGLE_CLIENT_ID=97759291881-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
   GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret-here
   GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback
   ```

3. ファイルを保存

**⚠️ 重要**: `.env` ファイルは `.gitignore` に登録されており、Git で管理されません。機密情報はこの方法で安全に管理できます。

### 3.2 アプリケーション実行中に設定する方法

ローカルに `.env` ファイルがない場合、またはユーザーが設定画面から更新したい場合は以下の手順に従います:

1. アプリケーションを起動
2. システムトレイ（画面右下）のアイコンをクリック
3. 「設定を開く」を選択
4. 「Google API クレデンシャル」セクションを表示
5. Client ID / Client Secret / Redirect URI を入力
6. 「保存」をクリック

設定は OS のユーザーデータ領域に永続化されます。

### 優先順位

認証情報は以下の優先順でロードされます（上から順）：

1. **`.env` ファイルの環境変数** (開発環境で推奨)
2. **アプリケーション設定画面で保存した値** (ユーザー設定)

`→ .env` が存在する場合はそちらが優先されます。アプリケーション設定画面で入力した値は、`.env` がない場合に使用されます。

## ステップ4: トレイアイコンの準備（任意）

1. 32x32または64x64サイズのPNG画像を用意
2. `public/icon.png` として保存

アイコンがない場合でも動作しますが、トレイアイコンが表示されない可能性があります。

## ステップ5: アプリケーションの起動

### 開発モードで起動

```bash
npm run dev
```

### 初回起動時の認証

1. アプリが起動します
2. 初回起動時に認証情報が未設定の場合は、設定画面を開くよう案内されます（またはシステムトレイから手動で開く）
3. 設定画面で Google API クレデンシャル（Client ID / Client Secret）を入力して保存するか、`.env` ファイルを事前に作成してください
4. 認証情報が設定されたら、「Googleアカウントでログイン」ボタンをクリック
5. ブラウザウィンドウが開き、Googleログイン画面が表示されます
6. Googleアカウントでログイン
7. アクセス許可を確認し、「許可」をクリック
8. 認証が完了すると、アプリにカレンダーとタスクが表示されます

## トラブルシューティング

### エラー: "invalid_client"
- クライアントIDとシークレットが正しく設定されているか確認
- Google Cloud Consoleで認証情報が正しく作成されているか確認

### エラー: "redirect_uri_mismatch"
- Google Cloud Consoleで `http://localhost:3000/oauth2callback` がリダイレクトURIとして登録されているか確認

### アプリが起動しない
- `npm install` が正常に完了したか確認
- Node.jsのバージョンが16以上であることを確認: `node --version`

### カレンダーやタスクが表示されない
- Google Calendar APIとGoogle Tasks APIが有効化されているか確認
- インターネット接続を確認
- ブラウザの開発者ツール(F12)でエラーログを確認

### トレイアイコンが表示されない
- `public/icon.png` が存在するか確認
- 画像形式がPNGであることを確認

## 次のステップ

アプリケーションが正常に起動したら:

1. **コントロールパネルの使用**: 画面上部にマウスを合わせると操作パネルが表示されます
2. **表示の切り替え**: カレンダー/タスク、月/週/日表示を切り替えられます
3. **イベントの詳細**: イベントやタスクをクリックすると詳細が表示されます
4. **設定画面**: システムトレイのアイコンをクリックして設定画面を開けます

## 開発を続ける場合

- `src/renderer/components/` - UIコンポーネントの編集
- `src/renderer/App.tsx` - メインアプリのロジック
- `src/main/services/` - Google APIとの連携ロジック
- `src/renderer/App.css` - スタイルの調整

変更を加えた後は、ホットリロードが自動的に適用されます。

## ビルドと配布

本番環境向けにビルドする場合:

```bash
npm run build
```

ビルドされたファイルは `dist/` ディレクトリに出力されます。

---

質問や問題がある場合は、READMEのトラブルシューティングセクションを参照してください。
